# Build context bloat
.git/
target/

# Local user settings (JSONC)
settings.json
spotfreeze.json
spotfreeze.jsonc

# Editor / OS cruft
.idea/
.vscode/
*.swp
.DS_Store

# Not needed inside the image; keeps source-layer cache stable
Dockerfile
docker-compose.yml
.github/
packaging/
